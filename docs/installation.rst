============
Installation
============

At the command line::

    $ easy_install pytextparser

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pytextparser
    $ pip install pytextparser
